<div class="profile-container">
  
  <%- include('../partials/acct-nav.ejs') %>

  <main class="profile-display">
    <div class="profile-account">
      <p class="profile-title">Comments</p>
    </div>

    <section class="profile-content">
      <% // favorites.forEach(favorite => { %>
        <p class="content-title"><a href="/listings/<%= // favorite.page_id %>"><%= // favorite.title %></a></p>
        <p class="content-author">Posted by - u/<%= // favorite.author %></p>
      <% // }) %>
    </section>
  </main>
</div>